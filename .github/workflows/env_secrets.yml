name: Demo CI Env Sercrets

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Install
        env:
          APP_ENV: demo
        run: echo "ENV $APP_ENV"
